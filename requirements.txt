# ─────────────────────────────────────────────────────────────────────────────
# MonoTrack — Shuttle Z-Trajectory Pipeline
# requirements.txt


# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0


# ── Computer vision ───────────────────────────────────────────────────────────
# opencv-python  = standard build  (GUI windows work, needed for Module 1 GUI)
# opencv-python-headless = no GUI  (use this on servers / CI)
# Pick exactly ONE of the two lines below:
opencv-python>=4.8.0
# opencv-python-headless>=4.8.0

# ── Module 2: TrackNetV3 runner ───────────────────────────────────────────────
# TrackNetV3 is called as a subprocess via its own predict.py.
# Its dependencies (torch, etc.) must be installed separately according to
# your GPU / CUDA version.  The lines below cover the CPU-safe minimum that
# most TrackNet repos expect:
torch>=2.0.0          # TrackNetV3 inference
torchvision>=0.15.0   # image transforms used by TrackNet


# ── Module 3: pose estimation ─────────────────────────────────────────────────

# RTMPose via mmpose 
# Install separately with mim (NOT pip) to get correct mmcv build:
  pip install -U openmim
  mim install mmcv "mmdet>=3.0" "mmpose>=1.0"

# ── Runtime utilities (transitive, pinned for reproducibility) ───────────────
pillow>=9.0.0          # matplotlib image I/O
packaging>=21.0        # version checks inside mmpose / torch
parse>=1.0.0           # for parsing mmpose config files

# ─────────────────────────────────────────────────────────────────────────────
# Python version requirement: = 3.10
# (dataclasses with field(), match/case patterns, and union type hints
#  X | Y are used in the modules)
# ─────────────────────────────────────────────────────────────────────────────